package packages.inutile;

import org.apache.commons.lang3.ArrayUtils;

public class Push {
	/*public Live[] push(Live[] array, Live push) {
		Live[] longer = new Live[array.length + 1];
        for (int i = 0; i < array.length; i++) longer[i] = array[i];
        longer[array.length] = push;
        return longer;
    }
	public Live[] removeIndex(Live[] array, int remove){
		return ArrayUtils.removeElement(array, remove);
	}
	public String[] shift(String[] array, String shift){
		String[] shifted = new String[array.length];
		shifted[0] = shift;
		for(int i = 1; i < shifted.length; i++) shifted[i] = array[i - 1];
		return shifted;
	}*/
}
